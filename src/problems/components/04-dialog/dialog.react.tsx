import css from './dialog.module.css'
import flex from '@course/styles'
import cx from '@course/cx'

type TProps = {}

export const Dialog = (props: TProps) => {
  return <div>TODO: Implement</div>
}
