import css from './calculator.module.css'
import flex from '@course/styles'
import cx from '@course/cx'

type TProps = {}

export const Calculator = (props: TProps) => {
  return <div>TODO: Implement</div>
}
